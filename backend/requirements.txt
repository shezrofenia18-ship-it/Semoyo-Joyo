# Semoyo Joyo B2B API - runtime dependencies (production / Docker build)
# Hanya pustaka yang benar-benar di-import oleh kode backend. Semua paket tersedia di PyPI
# sebagai wheel siap pakai (tidak butuh compiler / libpq-dev). Dev-only tools ada di requirements-dev.txt.

# Web framework & server
fastapi==0.110.1
starlette==0.37.2
uvicorn[standard]==0.25.0
python-multipart==0.0.32

# Validasi / konfigurasi
pydantic==2.13.5
python-dotenv==1.2.3

# Database (PostgreSQL, SQLAlchemy 2 async + asyncpg)
SQLAlchemy==2.0.52
asyncpg==0.31.0
greenlet==3.5.5

# Auth
PyJWT==2.13.0
bcrypt==4.1.3

# HTTP client (Midtrans API, seed scripts)
httpx==0.28.1

# Laporan & file
openpyxl==3.1.5
reportlab==5.0.1
pillow==12.3.0
